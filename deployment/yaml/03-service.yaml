apiVersion: v1
kind: Service
metadata:
  name: libraryweb
  namespace: wcare-ns
  labels:
    app: wcare-application
    subname: libraryweb
spec:
  type: ClusterIP
  ports:
    - name: 8080-tcp
      port: 8080
      targetPort: 8080
  selector:
    app: wcare-application
    subname: libraryweb